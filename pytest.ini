[pytest]
testpaths = tests
# Show why each xfail is expected; every one corresponds to an entry in
# tests/known_defects.py.
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
