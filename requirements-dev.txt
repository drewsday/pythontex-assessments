# Dependencies for running the test suite (`python -m pytest tests/`).
#
# These are only needed to test the Python embedded in the assessments. Building
# a PDF needs a TeX installation and pythontex instead -- see README.md.
pytest>=7.4

# Several assessments still do `from pylab import *`, so the blocks will not
# execute without matplotlib and numpy present.
matplotlib>=3.7
numpy>=1.24

# TestingScope.tex imports sympy.
sympy>=1.12

# randassign is NOT listed here on purpose: tests/pytexlib.py installs a stub so
# the suite stays hermetic and does not write solution files as a side effect.
